{
    "tasks": [
        {
            "type": "shell",
            "label": "CMake: 构建项目",
            "command": "cmake --build build",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": false
            },
            "detail": "使用 CMake 构建整个项目"
        }
    ],
    "version": "2.0.0"
}